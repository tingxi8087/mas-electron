# mas-electron

一个基于 Electron + React + TypeScript + Vite 的现代化桌面应用开发模板。

## ✨ 特性

- 🚀 **快速开发** - 基于 Vite 的极速热重载
- ⚛️ **React 18** - 使用最新的 React 特性
- 📘 **TypeScript** - 完整的类型支持
- 🎨 **Tailwind CSS** - 现代化的样式解决方案
- 🎯 **Ant Design** - 丰富的 UI 组件库
- 🔧 **开发工具** - ESLint、TypeScript 严格模式
- 📦 **多平台构建** - 支持 macOS、Windows、Linux
- 🔐 **代码压缩** - 生产环境自动压缩优化

## 🛠️ 技术栈

- **Electron** - 跨平台桌面应用框架
- **React** - UI 框架
- **TypeScript** - 类型安全的 JavaScript
- **Vite** - 下一代前端构建工具
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Ant Design** - 企业级 UI 设计语言
- **React Router** - 声明式路由
- **electron-builder** - 应用打包工具

## 📋 前置要求

- Node.js >= 18.0.0
- npm 或 yarn 或 bun

## 🚀 快速开始

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
bun install
```

### 开发模式

启动开发服务器（同时启动 Web 和 Electron）：

```bash
npm start
```

或者分别启动：

```bash
# 启动 Web 开发服务器
npm run dev:web

# 启动 Electron（需要另一个终端）
npm run dev:electron
```

### 构建应用

构建所有平台：

```bash
npm run build
```

构建特定平台：

```bash
# macOS
npm run build:mac

# Windows
npm run build:win
```

构建产物将输出到 `build` 目录。

## 📁 项目结构

```
mas-electron/
├── src/
│   ├── electron/          # Electron 主进程代码
│   │   ├── main.ts        # 主进程入口
│   │   ├── MainWindow.ts  # 窗口管理
│   │   ├── preload.ts     # 预加载脚本
│   │   └── store.ts       # 主进程状态管理
│   ├── components/        # React 组件
│   ├── views/            # 页面视图
│   ├── router/           # 路由配置
│   ├── store/            # 状态管理
│   ├── http/             # HTTP 请求封装
│   ├── utils/            # 工具函数
│   ├── types/            # TypeScript 类型定义
│   ├── layout/           # 布局组件
│   ├── assets/           # 静态资源
│   ├── main.tsx          # React 应用入口
│   ├── global.less       # 全局样式
│   └── tailwind.css      # Tailwind CSS
├── scripts/              # 构建脚本
├── dist/                 # 构建输出目录
├── build/                # 打包输出目录
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 项目配置
```

## 🔧 开发指南

### 环境变量

项目支持环境变量配置，可以通过 `.env.localhost` 文件配置开发环境变量。

### 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 使用 JSDoc 格式的注释
- React 组件使用函数式组件

### 路径别名

项目配置了路径别名 `@`，指向 `src` 目录：

```typescript
import Component from '@/components/Component'
```

## 📦 构建说明

构建过程分为三个步骤：

1. **编译 Web 代码** - 使用 Vite 构建 React 应用
2. **编译 Electron 代码** - 使用 TypeScript 编译主进程代码
3. **压缩优化** - 使用 Terser 压缩 Electron 代码

最终使用 `electron-builder` 打包成可执行文件。

## 🎯 支持的平台

- **macOS** - DMG 格式，支持 x64 和 arm64
- **Windows** - NSIS 安装程序，支持 x64 和 ia32
- **Linux** - AppImage 和 deb 格式

## 📝 许可证

本项目采用 MIT 许可证。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📮 联系方式

如有问题或建议，请通过 GitHub Issues 联系。
